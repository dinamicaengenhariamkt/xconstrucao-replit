node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Next.js
.next
.next/dev/*
out
*.tsbuildinfo
next-env.d.ts

# Replit config with sensitive credentials
.replit

# Environment files with secrets (NUNCA versionar .env.local — só o .env.example)
.env
.env.local
.env*.local

# Claude Code (.claude/ e CLAUDE.md SÃO versionados de propósito — skills locais)
# Skills e estado do agente — excluir do scan do Tailwind v4 (não é código da app)
.local

# Playwright
.next-e2e
test-results/
playwright-report/
.next-prod

# Turbopack incremental build cache (never commit — can reach several GiB)
.next-sweep
