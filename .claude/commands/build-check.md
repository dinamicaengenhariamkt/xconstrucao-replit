---
description: Roda tsc no projeto via build-validator e reporta erros
---

Invoque o agent `build-validator` para rodar `npm run check` no projeto e reportar qualquer erro de TypeScript com link clicável pro arquivo:linha.

Se passou: confirme com 1 linha.
Se falhou: liste os erros agrupados por arquivo, com sugestão de fix curta.
