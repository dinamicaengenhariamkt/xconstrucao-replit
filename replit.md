# XConstrução - Construction Management Platform

## Overview
XConstrução is a comprehensive construction management platform that connects contractors (contratantes) with builders (empreiteiros). Built with Next.js 16 App Router + PostgreSQL (Drizzle ORM).

## Architecture
- **Framework**: Next.js 16 App Router (server/client components)
- **Frontend**: React 19 + TanStack Query + shadcn/ui + Tailwind CSS v4
- **Backend**: Next.js API Route Handlers (app/api/)
- **Database**: PostgreSQL via Drizzle ORM
- **Auth**: JWT tokens in httpOnly cookies
- **Font**: Manrope (Google Fonts)
- **Primary Color**: dark gray #333333
- **Port**: 5000 (bound to 0.0.0.0)

## Key Files
- `shared/schema.ts` - Database schema + Zod validation
- `server/auth.ts` - JWT authentication utilities
- `server/storage.ts` - Database operations (IStorage interface)
- `server/db.ts` - Database connection
- `server/seed.ts` - Seed data
- `server/index.ts` - Entry point (spawns Next.js dev server)
- `instrumentation.ts` - Next.js instrumentation (runs seed on startup)
- `app/layout.tsx` - Root layout with Manrope font
- `app/globals.css` - Tailwind v4 config with @theme blocks
- `app/page.tsx` - Landing page
- `app/login/page.tsx` - Login page
- `app/register/page.tsx` - Registration page
- `app/dashboard/layout.tsx` - Dashboard layout with sidebar + auth guard
- `app/dashboard/page.tsx` - Dashboard stats
- `app/dashboard/clientes/page.tsx` - Clients CRUD
- `app/dashboard/empreiteiras/page.tsx` - Contractors CRUD
- `app/dashboard/obras/page.tsx` - Projects CRUD
- `app/dashboard/financeiro/page.tsx` - Financial records CRUD
- `app/api/` - All API route handlers
- `components/app-sidebar.tsx` - Sidebar navigation
- `components/providers.tsx` - Client providers (QueryClient, Auth, Theme)
- `lib/auth.tsx` - Auth context (client-side)
- `lib/queryClient.ts` - TanStack Query client + apiRequest helper

## API Routes
- `POST /api/auth/login` - Returns `{ user }` + sets cookie
- `POST /api/auth/register` - Returns `{ user }` + sets cookie
- `POST /api/auth/logout` - Clears cookie
- `GET /api/auth/me` - Returns user data
- `GET /api/dashboard/stats` - Dashboard statistics
- `GET/POST /api/clientes` - List/Create clients
- `GET/PUT/DELETE /api/clientes/[id]` - Single client operations
- `GET/POST /api/empreiteiras` - List/Create contractors
- `GET/PUT/DELETE /api/empreiteiras/[id]` - Single contractor operations
- `GET/POST /api/obras` - List/Create projects
- `GET/PUT/DELETE /api/obras/[id]` - Single project operations
- `GET/POST /api/financeiro` - List/Create financial records

## User Preferences
- All UI text in Portuguese (pt-BR)
- Currency formatted as BRL (R$)
- Multi-role system: admin, contratante, empreiteiro
- Clean, professional design with Manrope font
- Dark/light mode toggle

## Test Accounts (senhas fortes — política "balanced")
- Super Admin (auto-promovido): admin@xconstrucao.com / Admin@2026!Constru
- Contratante: joao@construtora.com / Joao@2026!Obras
- Empreiteiro: maria@empreiteira.com / Maria@2026!Reforma

## Política de Senha (Task #10)
- Mínimo 8 caracteres + 3 das 4 categorias (maiúscula/minúscula/número/especial).
- Bloqueia senhas comuns e que contenham email/nome/usuário.
- Implementação: `features/auth/schemas/password.ts` (`evaluatePasswordPolicy`, `passwordStrength`).
- Aplicada em registerSchema e endpoint `/api/auth/reset-password`. Login só exige `min(1)` (não quebra contas legadas).

## Anti-bot (Task #10)
- Honeypot DOM (`<HoneypotField>`) + timing min 1.5s (`useAntiBotPayload` / `validateAntiBot`).
- Aplicado em login, cadastro e recuperar-senha.
- Em login/registro a falha vira mensagem genérica; em forgot-password vira "sucesso" silencioso.

## Reset destrutivo + reseed
- `scripts/reset-and-seed.ts` — em produção exige `CONFIRM_PROD_RESET=YES`.

## Configuration
- `next.config.ts` - Next.js config (port 5000, allowedDevOrigins)
- `postcss.config.mjs` - PostCSS with @tailwindcss/postcss
- `tsconfig.json` - TypeScript with path aliases (@/, @shared/)
- Tailwind CSS v4 uses CSS-based config in globals.css (no tailwind.config.ts)

## Transactional Email (Brevo)
- Provider: **Brevo** via HTTP API `POST https://api.brevo.com/v3/smtp/email` (header `api-key`).
- Service module: `shared/lib/email.ts` exposes `sendVerificationEmail`, `sendWelcomeEmail`, `sendPasswordResetEmail`. Templates in `features/auth/emails/*.tsx` rendered with `@react-email/render`.
- Required secret: `BREVO_API_KEY` (Replit Secret). Required env: `EMAIL_FROM=noreply@dinamicareforma.com.br` (domain must be verified inside the Brevo account).
- Sender exibido: `XConstrução <noreply@dinamicareforma.com.br>`.
- Test mode: `EMAIL_TEST_MODE=1` → emails são capturados em memória (`shared/lib/test-email-store.ts`) e expostos por `/api/test/emails` para os E2E. A API da Brevo NÃO é chamada nesse modo.

## Feature Architecture (Empreiteiro & Contratante)
Feature-based architecture under `features/empreiteiro/` and `features/contratante/`:
- Each feature has: types/, mocks/, hooks/, api/, components/ folders
- Mock data enabled via `NEXT_PUBLIC_ENABLE_EMPREITEIRO_MOCK=true` (shared for both roles)
- React Query hooks with 5min stale time, Zustand for state (xchat)
- Shared components in `features/shared/components/` (PageHeader, FilterChips, StatusBadge, ProgressBar)
- Shared types in `features/shared/types/`

### Empreiteiro Pages
- `/empreiteiro/dashboard` - Stats overview, financial charts, recent activities
- `/empreiteiro/minhas-obras` - Active projects with status filters (em execução, com atrasos, pendências, planejamento, finalizadas)
- `/empreiteiro/novas-obras` - Available projects with complexity filters, blocked state banner
- `/empreiteiro/novas-obras/[id]` - Project detail with hero, accordion sections, 3 application states
- `/empreiteiro/chat` - Two-panel messaging (conversations list + messages area)
- `/empreiteiro/faq` - FAQ with hero search, category tabs, accordion items

### Empreiteiro Nav Constants
- `features/empreiteiro/constants.ts` - EMPREITEIRO_NAV_ITEMS, EMPREITEIRO_BOTTOM_NAV_ITEMS
- Sidebar: `features/empreiteiro/components/EmpreiteiroSidebar.tsx`

### Contratante Pages
- `/contratante/dashboard` - Stats overview (already complete, do not modify)
- `/contratante/minhas-obras` - Projects with status filters, grayscale→color hover cards
- `/contratante/minhas-obras/[id]` - Project detail with hero, KPIs, tabs (Visão Geral, Etapas, Financeiro, Equipe, Fotos)
- `/contratante/nova-obra` - Multi-card form (Info, Location, Timeline/Budget, Documents)
- `/contratante/pagamentos` - Financial KPIs, filters, transaction table with status badges
- `/contratante/chat` - Two-panel messaging with Zustand store
- `/contratante/faq` - FAQ with hero search, category tabs, accordion items

### Contratante Nav Constants
- `features/contratante/constants.ts` - CONTRATANTE_NAV_ITEMS (Nova Obra, Minhas Obras, Dashboard, Pagamentos, xchat), CONTRATANTE_BOTTOM_NAV_ITEMS (FAQ, Configurações)
- Sidebar: `features/contratante/components/ContratanteSidebar.tsx`

### Admin Pages
- `/admin/financeiro` - Financial dashboard with KPIs, charts, tables (already complete)
- `/admin/clientes` - Client list with filters, stats, search
- `/admin/clientes/[id]` - Client detail (cadastro info, KPIs, tabs)
- `/admin/clientes/[id]/obras` - Client's projects subpage
- `/admin/clientes/novo` - New client form (useForm + zodResolver)
- `/admin/empreiteiras` - Contractors list with filters, stats, search
- `/admin/empreiteiras/[id]` - Contractor detail (cadastro, KPIs, especialidades, rating)
- `/admin/empreiteiras/[id]/obras` - Contractor's projects subpage
- `/admin/empreiteiras/nova` - New contractor form (useForm + zodResolver)
- `/admin/caixa` - Cash flow overview with KPIs, period filter, movements table
- `/admin/entradas` - Income entries with category filters, search, table
- `/admin/saidas` - Expense entries with category filters, search, table
- `/admin/anuncios` - Ads management with KPIs, status filters, ad cards
- `/admin/faq` - FAQ management with category filters, search, accordion

### Admin Nav Constants
- `features/admin/constants.ts` - ADMIN_NAV_ITEMS (Cliente, Empreiteira, Financeiro, Caixa, Entradas, Saídas, Anúncios), ADMIN_BOTTOM_NAV_ITEMS (FAQ, Configurações)
- Sidebar: `features/admin/components/AdminSidebar.tsx`
- Topbar: `features/admin/components/AdminTopbar.tsx`
- Layout: `features/admin/components/AdminLayout.tsx`

## Convenções de planejamento
- **Toda task do board cita a jornada de origem** num cabeçalho no topo do plan file (`> Jornada: JXX — Nome (docs/jornadas/XX-...md)`). Quem abrir a task no futuro vai direto pro roteiro completo.
- **Gap fora de escopo descoberto durante a execução** → 1 linha datada (`AAAA-MM-DD (Task #N): ...`) na seção 13 "Gaps descobertos durante execução" do markdown da jornada correspondente. Não cria task nova só por isso — vira backlog rastreado por jornada.
- **Gap grande sem casa** → nova jornada numerada (14...) a partir de `docs/jornadas/_template.md` + atualizar índice de `docs/jornadas/README.md`.
- **Checklist da seção 9 de cada jornada** é fonte de verdade do que está pronto/falta. Ao fechar uma task marcar `[x]` com `_(Task #N)_` e atualizar o status no índice quando a jornada mudar de fase (`mock → parcial → revisão → pronto`).

## Super Admin & Gestão de Usuários (Task #20)
- **Superadmin = "admin++" nos gates administrativos** (Task #31): endpoints que historicamente faziam `role !== "admin"` literal foram migrados pro helper `isAdminLike(role)` em `features/auth/api/auth-utils.ts` (libera `admin` e `superadmin`, mantém `contratante`/`empreiteiro` bloqueados). Aplicado em: `app/api/perfil/admin/route.ts`, `app/api/admin/configuracoes/route.ts`, `app/api/admin/integracoes/api-key/route.ts`, `app/api/admin/clientes/[id]/aprovacao/route.ts`, `app/api/admin/empreiteiras/[id]/aprovacao/route.ts`. **Não migrado de propósito**: `app/api/auth/oauth-convert/route.ts:59` (semântica para superadmin não óbvia — registrar gap antes de mexer). Em rotas novas administrativas, **sempre** preferir `isAdminLike()` ao literal.
- **Bootstrap automático**: `server/bootstrap-superadmin.ts` roda em toda inicialização — adiciona enum `superadmin`, colunas `must_change_password`/`created_by`/`ativo`/`can_manage_users`, tabelas `audit_logs` + `password_setup_tokens`, e promove `admin@xconstrucao.com` para `superadmin` (idempotente).
- **Bootstrap CLI**: `npx tsx scripts/bootstrap-superadmin.ts --email <e> --name <n> [--password <p>] [--force-reset-password]` (também aceita env `SUPERADMIN_EMAIL`/`NAME`/`PASSWORD`/`FORCE_RESET=YES` ou prompts interativos). **Se nenhuma senha for informada (e o stdin não for um TTY), o script gera uma senha forte de 16 chars automaticamente, marca `must_change_password=true` e a imprime UMA ÚNICA VEZ no final** (use sempre que rodar em produção sem informar senha). Valida política, **gate estrito**: se já existe ≥1 super admin ativo, recusa criar/promover/reativar uma conta DIFERENTE sem `--force-reset-password` (operações idempotentes na mesma conta super existente seguem permitidas). Garante invariante de pelo menos 1 super admin ativo após a operação, **insere `user_consents` v1.0 (termos + privacidade) idempotentes** e grava `audit_logs` (`action="cli.bootstrap-superadmin"`, ip="cli").
- **Permissão "Gerenciar usuários"** (`can_manage_users`): super admin tem acesso implícito; admin precisa do flag (controlado por super admin no edit dialog). Aba Usuários e endpoints `/api/admin/usuarios*` aplicam o gate via `hasUsersTabAccess()`. `/api/auth/me` devolve `canManageUsers` para o front esconder a aba.
- **Aba Usuários**: `/admin/configuracoes?tab=usuarios` (`features/admin/components/UsuariosTab.tsx`) — listar/criar/editar (incluindo email com checagem 409)/desativar/resetar senha. Super admin gerencia todos; admin gerencia apenas contratante/empreiteiro (e somente se `can_manage_users=true`). Filtros (perfil, status), busca e paginação visíveis.
- **3 modos de senha** (criação ou reset): `random` (default — gera 16 chars + força troca), `manual` (define + checkbox "exigir troca no 1º login" default ON), `link` (e-mail Brevo com token URL-safe TTL 24h). **Em qualquer modo a credencial atual é invalidada imediatamente** — modo `link` grava um hash aleatório descartado e seta `must_change_password=true`, garantindo que a senha antiga deixa de funcionar no instante do reset/criação. Endpoint `/api/admin/usuarios` aceita `forceChangeOnFirstLogin` (default true; só desligável em modo manual).
- **Troca obrigatória 1º login**: `must_change_password=true` → `AuthSessionGuard` redireciona o front para `/trocar-senha-obrigatoria`; **enforcement no servidor** acontece em duas camadas — `requireVerifiedUser` rejeita 403 PASSWORD_CHANGE_REQUIRED em rotas que usam o guard, e `proxy.ts` (Edge) bloqueia GLOBALMENTE todas as rotas `/api/*` (lendo o claim `mustChangePassword` do JWT) com allowlist mínima (`change-password-forced`, `logout`, `me`, `refresh`, `definir-senha-inicial`). Endpoint `POST /api/auth/change-password-forced` valida política, remove flag, **persiste novo `sessions` row** (evita "Sessão revogada" no próximo refresh) e reemite cookies com `mustChangePassword:false`. `canManageUsers` e `mustChangePassword` viajam no payload de `/api/auth/login` e `/api/auth/refresh`.
- **Definição inicial via link**: `/definir-senha-inicial?token=...` consome token (sha256 hash em DB) e ativa conta. Endpoint `POST /api/auth/definir-senha-inicial`.
- **Modo "Ver como" (impersonation)**: `POST /api/admin/impersonate/[id]` (apenas super admin) emite cookie HMAC `impersonation_token` (TTL 1h). `requireVerifiedUser` substitui o `user` pelo target em verbos read-only. **Bloqueio global**: `proxy.ts` (Edge) intercepta TODAS rotas `/api/*` em verbos mutáveis e devolve **403 IMPERSONATION_READ_ONLY** sempre que o cookie `impersonation_token` está presente — cobre rotas legadas sem `requireVerifiedUser`. Layouts contratante/empreiteiro permitem `superadmin` quando impersonando, redirect por target.role. Banner persistente `ImpersonationBanner.tsx`. `POST /api/admin/impersonate/exit` encerra.
- **Conta inativa**: login devolve `403 ACCOUNT_DISABLED`; `requireVerifiedUser` bloqueia idem.
- **Audit log**: toda criação/edição/reset/impersonate é gravada em `audit_logs` (actor, action, target, payload, IP, UA).
- **Endpoints novos**:
  - `GET/POST /api/admin/usuarios`, `GET/PATCH/DELETE /api/admin/usuarios/[id]`
  - `POST /api/admin/usuarios/[id]/reset-password`, `POST /api/admin/usuarios/[id]/ativo`
  - `POST /api/admin/impersonate/[id]`, `POST /api/admin/impersonate/exit`
  - `POST /api/auth/change-password-forced`, `POST /api/auth/definir-senha-inicial`
- **Helpers**: `features/auth/api/{audit,password-generator,impersonation,password-setup-tokens}.ts`.

## Cloudflare R2 + Uploads (Task #26)
- **Storage layer genérico**: `shared/lib/storage/r2.ts` (S3Client v3, presign PUT, sign GET, delete, head), `key-builder.ts` (rotas: `public/avatars/{role}/{userId}/...`, `public/empreiteiro/{userId}/portfolio/...`, `private/empreiteiro/{userId}/documentos/{tipo}/...`), `validation.ts` (`KIND_RULES` com mime/limite por kind: avatar 2MB, portfolio 8MB, documento 15MB).
- **Schema**: tabela `user_files` (kind, visibility public/private, key, mime, sizeBytes, ownerUserId) + `empreiteiro_documentos` (fileId, tipo, observacao). Bootstrap idempotente em `server/bootstrap-storage.ts` (chamado por `instrumentation.ts`).
- **API**:
  - `POST /api/uploads/presign` — devolve `{ url, key, headers }` (presign PUT 5min, valida kind/mime/size, anti-tamper checa userId na key).
  - `POST /api/uploads/commit` — HEAD R2 para confirmar upload + cria `user_files` row + side-effects (avatar→atualiza `users.avatarUrl` + perfil empreiteira/cliente; documento→cria `empreiteiro_documentos` + audit log; portfolio sem side-effect — page chama `updatePerfil`).
  - `GET /api/uploads/sign?id=...` e `GET /api/uploads/[id]` (signed URL TTL 15min, valida ownership ou superadmin).
  - `DELETE /api/uploads/[id]` — remove no R2 + DB + audit (`uploads.delete.documento`).
  - `GET /api/perfil/empreiteiro/documentos` — lista documentos do empreiteiro logado com signed URLs.
- **Hook + UI**: `features/shared/hooks/use-uploads.ts` (`useUpload` com XHR PUT + progresso, `deleteUpload`), `features/shared/components/FileUploader.tsx`, `features/perfil/hooks/use-documentos.ts` (`useEmpreiteiroDocumentos`, `useDeleteDocumento`).
- **Páginas migradas**:
  - `/empreiteiro/configuracoes` — avatar via R2, portfolio (imagens) via R2 + `updatePerfil({portfolioUrls})`, portfolioDocs stamped `${name}::${url}`. Nova aba **Documentos** (`SecaoDocumentos`) lista privada com upload (tipo + observação), baixar (link signed) e remover.
  - `/contratante/configuracoes` — avatar via R2 (`fileToDataUrl` removido).
  - `/admin/configuracoes` — avatar via R2 (botão "Enviar foto" ao lado do `<Avatar>`).
- **Secrets necessários** (Replit Secrets, NUNCA logar valores): `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`, `R2_S3_ENDPOINT`, `R2_PUBLIC_BASE_URL`.
- **Segurança**: presign + commit dependem de `requireVerifiedUser`. Em impersonation, `proxy.ts` (Edge) já bloqueia mutações `/api/*` com 403 IMPERSONATION_READ_ONLY. Documentos privados nunca expõem URL pública — sempre via signed URL com TTL curto.

## Mocks & Dev offline (Task #85)
- **Default: OFF em todos os módulos.** Banco real é a verdade em dev e prod.
- Helper único `isMockEnabled()` em `features/shared/lib/mock-flag.ts` — lê `NEXT_PUBLIC_ENABLE_EMPREITEIRO_MOCK === "true"` (default false). Todos os 21 callsites (constants/hooks de admin, contratante, empreiteiro e shared) passam por ele.
- Para rodar offline com dados mock: setar `NEXT_PUBLIC_ENABLE_EMPREITEIRO_MOCK=true` no `.env.local`. **Nunca ligar em produção** — usuários veriam dados falsos.
- Mocks (arquivos em `features/**/mocks/`) permanecem — só deixam de ser consumidos por default. Removê-los é trabalho futuro por módulo.
- `features/contratante/dashboard/constants.ts` tinha `ENABLE_MOCK = true` hardcoded (ignorando a env) — corrigido. `features/admin/disputas/constants.ts` usava default invertido (`!== 'false'`) — agora segue o helper (default OFF).

## Recent Changes
- 2026-05-26: Task #87 (J02) — Zona de atuação do empreiteiro habilita matching "Na minha zona" no marketplace. **Schema**: 2 colunas em `empreiteiras` (`zona_atuacao_ufs text[] DEFAULT ARRAY[]::text[]`, `zona_atuacao_cidades text[] DEFAULT ARRAY[]::text[]`) via `server/bootstrap-empreiteiras-zona.ts` (idempotente, registrado em `instrumentation.ts` antes do `bootstrap-obras`). **API**: `PATCH /api/perfil/empreiteiro` aceita `zonaAtuacaoUfs` (regex `^[A-Z]{2}$` + uppercase + dedupe + max 27) e `zonaAtuacaoCidades` (trim, 2–80 chars, dedupe case-insensitive, max 50). `GET /api/obras` (role=empreiteiro) carrega a zona da empreiteira logada e devolve `naMinhaZona: boolean` por linha — match SQL `UPPER(obras.uf)=ANY(ufs) OR LOWER(TRIM(obras.cidade))=ANY(cidades)`. Zona vazia → flag = `false` para todas. Em outras roles, o campo é stripado do payload. **UI**: `/empreiteiro/configuracoes` (aba Empresa) ganha seção **Zona de atuação** abaixo de Especialidades, com 2 `MultiSelectAdd`: UF (suggestions = `UF_OPTIONS`, validação client-side rejeita não-UF) + Cidades (tag-input livre). Form persiste no mesmo Salvar empresa via `updatePerfil`. `NovaObraCard` ganha selo verde "Na minha zona" (`bottom-left`, `bg-emerald-500/90`, `RiMapPin2Line`) quando `obra.naMinhaZona === true` — `data-testid="badge-na-minha-zona-{id}"`. **Tipos**: `PerfilEmpreiteiro`, `DbObra`, `NovaObra` ganham os novos campos; adapter `dbToNovaObra` propaga `naMinhaZona`. **OOS** (registrado em J02 §13): geo/raio em km, toggle "só na minha zona" + ordenação, normalização IBGE de cidades, notificação J13. J02 §9 ganha linha `[x] Zona de atuação ... _(Task #87)_`; §13 marca gap antigo de Task #41 como _Resolvido parcialmente_ inline + 2 bullets novos datados.
- 2026-05-26: Task #86 (J03 → pronto) — Gate de moderação admin antes de obra publicada entrar no marketplace. **Schema**: enum `obra_status_moderacao` (pendente/aprovada/rejeitada) + colunas `statusModeracao` (default pendente), `motivoModeracao`, `moderadoEm`, `moderadoPor` (FK users) em `obras`, índice `idx_obras_status_moderacao`. Bootstrap idempotente em `server/bootstrap-obras.ts` com backfill one-shot `obras_backfill_moderacao_v1` (publicada+pendente→aprovada, evita que obras pré-existentes sumam do marketplace). **Endpoints**: `POST /api/admin/obras/[id]/aprovar` (seta aprovada + emite atividade J07 `obra_publicada` na primeira aprovação + audit `obras.moderar.aprovar`); `POST /api/admin/obras/[id]/rejeitar` (body `motivo` 5–500 chars + audit `obras.moderar.rejeitar`); ambos gated por `isAdminLike` e exigem `visibilidade='publicada'`. PATCH `/api/obras/[id]` agora reseta moderação→pendente sempre que `visibilidade` transita para `publicada` (cobre publicação inicial e re-submissão pós-rejeição). GET `/api/obras` (empreiteiro) e `findObraWithAccess` agora exigem `statusModeracao='aprovada'`. GET `/api/admin/obras` aceita filtro `?status_moderacao=`. A atividade J07 `obra_publicada` foi movida de PATCH/[id] pro endpoint de aprovação. **UI admin**: nova página `/admin/obras/moderacao` com 3 tabs (Em revisão / Rejeitadas / Aprovadas), cards com aprovar/rejeitar (modal motivo), "aprovar mesmo assim" para rejeitadas; nav item `Moderação` em `ADMIN_NAV_ITEMS`. **UI contratante**: banner de status no detalhe (`ObraVisibilidadeActions`) — amarelo "Aguardando aprovação", vermelho "Rejeitada + motivo" com CTA, verde "Aprovada"; botão "Reenviar para moderação" pausa+republica em sequência. J03 §9 ganhou seção "Moderação admin" toda `[x]`; §13 marca o gap antigo de Task #33 como _Resolvido_ inline + 3 bullets novos datados (notificação→J13, auto re-moderação de edits in-place, IA fora de escopo). J03 promovida `pronto → pronto` (já estava, agora com o gate fechado).
- 2026-05-26: Task #85 (J04+J02+global) — Mocks desligados por default. Novo helper `isMockEnabled()` em `features/shared/lib/mock-flag.ts`; 21 callsites migrados (constants e hooks de admin/contratante/empreiteiro/shared). `.env.local` muda pra `NEXT_PUBLIC_ENABLE_EMPREITEIRO_MOCK=false` com comentário de opt-in pra dev. Bugs corrigidos: `features/contratante/dashboard/constants.ts` (era `true` hardcoded), `features/admin/disputas/constants.ts` (era `!== 'false'` invertido). Mocks NÃO deletados — apenas dormem por default. J04 §9 item "Remover mocks + flag" marcado `[x]` (critério "marketplace funciona sem mock" passa); gap registrado em J04 §13 e replit.md ganhou seção "Mocks & Dev offline".
- 2026-05-25: Task #84 (J06) — `AtualizarProgressoModal` (empreiteiro, abre via tarefa → "atualizar progresso") agora emite **medição real** via `POST /api/empreiteiro/medicoes`. Modal ganhou prop `obraId` + `useMutation` interna que envia `{ obraId, etapa: tarefa.etapa || tarefa.titulo (slice 120), descricao: "Atualização de progresso da tarefa \"X\" (A% → B%).", percentual: delta }`. **Regra**: delta = `valor - tarefa.progresso`; delta ≤ 0 → toast "Sem aumento de percentual" e NENHUMA medição criada (mas tarefa segue atualizando via `onConfirmar` → `useUpdateTarefa` do parent, Task #76). delta > 0 → POST + toast "Medição enviada para aprovação". Erros do server (incluindo 422 SUM>100, 429 rate limit) viram toast destructive com `err.message`. Modal não fecha em pending, botão Cancelar disabled, slider/atalhos disabled. Invalida `['empreiteiro','medicoes']`, `['contratante','medicoes']`, `['empreiteiro','minhas-obras',obraId]`, `['contratante','minhas-obras',obraId]`, `['obras',obraId]`. Tarefa atualiza em paralelo (fire-and-forget pelo parent). Gap antigo J06 §13 linha "Modal AtualizarProgressoModal ainda escreve em store local" marcado como _Resolvido (Task #84)_ inline. Nenhuma store local existia de fato — a alegação vinha de antes do Task #76 ter migrado o TaskManagerSection pra TanStack; o modal só faltava o emissor de medição. Sem mudanças de schema/endpoint, só camada de dados no front.
- 2026-05-25: Task #83 (J07 → pronto) — Atividades & Timeline reais ponta-a-ponta. **Schema** em `shared/db/schema.ts`: enum `atividade_tipo` (13 valores — `obra_publicada`, `candidatura_{criada,aceita,rejeitada,cancelada}`, `medicao_{criada,aprovada,contestada}`, `diario_postado`, `ocorrencia_{aberta,resolvida}`, `lancamento_{criado,quitado}`) + tabela `atividades` (id/tipo/actorUserId/obraId/targetUserId/payload jsonb/createdAt). Bootstrap idempotente em `server/bootstrap-atividades.ts` (5 índices: `(obra_id, created_at DESC)`, `(actor_user_id, created_at DESC)`, `(tipo, created_at DESC)`, `(target_user_id, created_at DESC)`, `(created_at DESC)`) registrado em `instrumentation.ts` antes de `markOverduePagamentos`. Emissores de candidatura populam `target_user_id` (criada→contratante via `clientes.userId`; aceita/rejeitada→empreiteiro via `empreiteiras.userId`; cancelada→contratante; cascade de rejeitadas no aceite resolve em batch via `inArray`). **Helper** `features/atividades/api/registrar.ts` aceita `tx?: typeof db | tx-do-transaction` — quando passado roda DENTRO da transação e propaga erro (invariante hard: aprovação de medição inclui `medicao_aprovada` + `lancamento_criado` na MESMA tx que cria a fatura); sem `tx`, falhas viram `console.error` silencioso (best-effort estilo audit). **Plugado em 12 endpoints**: `PATCH /api/obras/[id]` (só na transição rascunho→publicada), `POST /api/empreiteiro/candidaturas`, `POST /api/contratante/candidaturas/[id]/aceitar` (1 aceita + cascade N rejeitadas com `payload.cascata=true`), `POST /api/contratante/candidaturas/[id]/rejeitar`, `POST /api/empreiteiro/candidaturas/[id]/cancelar`, `POST /api/empreiteiro/medicoes`, `POST /api/contratante/medicoes/[id]/aprovar` (DENTRO da tx, 2 rows), `POST /api/contratante/medicoes/[id]/contestar`, `POST /api/obras/[id]/diario`, `POST /api/obras/[id]/ocorrencias`, `POST /api/obras/[id]/ocorrencias/[ocorrenciaId]/resolver`, `POST /api/contratante/pagamentos/[id]/quitar`, `POST /api/financeiro` (só quando `medicaoId` nulo — evita duplicar com hook de aprovação). **Endpoint** `GET /api/atividades?limit=&obraId=&tipo=&cursor=` cursor-based (`base64url(iso|id)`, ORDER BY `(created_at,id) DESC`, `limit+1` para `hasMore` + `nextCursor`) com gate por persona: admin/superadmin tudo; contratante = `actor=$me OR target=$me OR EXISTS clientes.user_id=$me`; empreiteiro = `actor=$me OR target=$me OR EXISTS empreiteiras.user_id=$me` (`target_user_id` cobre candidaturas decididas em obras ainda não atribuídas à empreiteira). **Hooks** `features/atividades/hooks/use-atividades.ts` (`useAtividadesRecentes` widget / `useAtividadesObra` timeline) + `toAtividadeDisplay(item, viewerRole)` (TIPO_META para titulo/icon/color/tipoTimeline + describePayload pt-BR) + `formatRelativeBr` ("agora há pouco", "há 5 min", etc). **UI rewired**: `features/empreiteiro/dashboard/api/activities-service.ts` reescrito para chamar `/api/atividades` + mapear pra `Activity[]` (mock deletado `features/empreiteiro/dashboard/mocks/activities.mock.ts`); `app/contratante/dashboard/page.tsx` usa `useAtividadesRecentes(10)` + adapta pra `ContratanteActivity[]` (mock de pendencias preservado, só `mockActivities` foi trocado); `features/empreiteiro/minhas-obras/components/TimelineSection.tsx` agora aceita `{obraId, fallbackEvents}` e hidrata via `useAtividadesObra`; `features/contratante/minhas-obras/components/TabTimeline.tsx` combina `TimelineDisplay` (J07) + `DiarioJ06Card` (J06) — atividade `diario_postado` é evento, conteúdo do diário fica em `obra_diario`. **Decisões**: (a) admin/auditoria fica em `audit_logs` separado (mais granular técnico, forense) — não unificar; (b) cascade de rejeitadas emite N rows com `cascata:true` no payload pra front agrupar visualmente; (c) chat/J13 propositalmente fora do feed; (d) `/contratante/atividades` ("Ver todas") segue mockada — gap rastreado J07 §13. J07 promovida `mock → pronto` no índice; §9 toda marcada exceto última, §13 com 7 bullets datados.
- 2026-05-25: Task #82 (J08 → pronto) — Hook J06→J08 ponta-a-ponta: aprovar medição cria fatura automaticamente. **Endpoint** `POST /api/contratante/medicoes/[id]/aprovar` reescrito para envolver `UPDATE medicoes SET status='aprovada'` + `criarLancamentoFromMedicao()` numa `db.transaction()` — se qualquer um falhar, nada persiste (sem medição aprovada sem fatura, e vice-versa). Resolve `pagadorUserId` via `obras.clienteId → clientes.userId`; `recebedorUserId = medicao.empreiteiroId`; valor = `medicao.valor` se >0, senão `obra.valorTotal × percentual / 100`; vencimento = aprovação UTC + 15 dias (`VENCIMENTO_DIAS_PADRAO`); descrição `Medição #{numero} — {etapa}`; categoria fixa `Medição`. **Idempotência** garantida por `CREATE UNIQUE INDEX uq_financeiro_medicao_unique ON financeiro(medicao_id) WHERE medicao_id IS NOT NULL` (parcial, adicionado em `server/bootstrap-pagamentos.ts`) + try/catch `23505` em `criarLancamentoFromMedicao()` que devolve a fatura existente em vez de errar — retry/duplo-clique nunca duplica. **Helper** `criarLancamentoFromMedicao()` ganhou parâmetro opcional `tx` (`typeof db | tx-do-transaction`) pra trabalhar dentro de `db.transaction()`. **Audit** agora grava 2 eventos por aprovação: `medicoes.aprovar` (preexistente, ampliado com `lancamentoId` no payload) + `financeiro.criar.from-medicao` (novo, payload com `lancamentoId`/`valor`/`dataVencimento`) — J13 vai ouvir esse pra notificar empreiteiro. **Decisões**: (a) `recomputeObraProgresso(obraId, tx?)` ganhou param opcional e roda DENTRO da transação — atomicidade total (medição aprovada ↔ fatura ↔ progresso recalculado); (b) invariante "toda aprovação gera fatura" é hard-fail 422 antes da transação — obra sem contratante vinculado → 422 `OBRA_SEM_CONTRATANTE_USER`, valor zerado → 422 `VALOR_INVALIDO`; aprovação **nunca** passa sem `lancamentoId`; (c) gateway de pagamento, estorno e endpoint avulso de lançamento manual ficam como gaps J08 §13. **Docs**: J08 §9 fecha (3 últimos itens), §13 ganha 5 bullets datados (transação, regra valor/vencimento, UNIQUE+23505, edge cases, gap manual); J06 §9 marca "Gerar lançamento financeiro (J08) no aprovar" como `[x] _(Task #82)_`; índice promove J08 `revisão → pronto`.
- 2026-05-25: Task #76 (J11) — Tarefas / Checklists / Equipe ponta-a-ponta na obra do empreiteiro. **Schema novo** em `shared/db/schema.ts` (4 tabelas + 6 enums + tipos): `obra_tarefas` (titulo/etapa/etapaId opcional→`obra_etapas`/responsavel/prazo/status pendente|em_andamento|bloqueado|concluido/prioridade alta|media|baixa/progresso 0-100/bloqueioMotivo/bloqueioInfo), `obra_checklists` (nome/tipo seguranca|diario|etapa/status pendente|em_andamento|completo/assinatura) + `obra_checklist_itens` (titulo/concluida/ordem), `obra_equipe` (nome/papel/tipo contratante|engenheiro|mestre|equipe/cor/contato/membros/ativo/permissao visualizar|editar|admin). Bootstrap idempotente em `server/bootstrap-obra-operacao.ts` registrado em `instrumentation.ts`. **6 endpoints novos** (todos `requireVerifiedUser` + `findObraAccess` + `canWriteObraContent` + `recordAudit` + `setNoCacheHeaders`): `GET/POST /api/obras/[id]/tarefas`, `PATCH/DELETE /[tarefaId]` (auto-coerência status=concluido→progresso=100), `GET/POST /api/obras/[id]/checklists`, `PATCH/DELETE /[checklistId]` (suporta `toggleItemId` atômico, `markAllItens` para finalizar/assinar, e substituição completa de `itens` em edição), `GET/POST /api/obras/[id]/equipe`, `PATCH/DELETE /[membroId]`. **Build server** `features/empreiteiro/minhas-obras/api/build-detalhe-server.ts` agora hidrata `tarefas` direto de `obra_tarefas` (e ainda mapeia tarefas com `etapaId` pro array de cada `etapa`), `checklists` com itens e progresso recalculado, e `equipe` mesclando membros reais do DB com 2 entradas virtuais derivadas (contratante + empreiteira responsável, com prefixo `contratante-`/`empreiteira-` no id pra impedir edição). **Hooks** `features/empreiteiro/minhas-obras/hooks/use-obra-operacao.ts` com `useCreate/Update/Delete` × {Tarefa, Checklist, Membro} + helper `isVirtualMembroId`; invalidam `['empreiteiro','minhas-obras',obraId]` (e `['contratante','minhas-obras',obraId]`/`['admin','obras',obraId]` por reuso futuro) com toasts pt-BR padronizados. **3 sections rewired** (`TaskManagerSection`, `ChecklistsSection`, `EquipeSection`): removido `useState<...[]>(obra.X)`, lista derivada direto do prop `obra.X` (server state), todos os handlers (criar/editar/duplicar/excluir + transições status/bloqueio/progresso + toggle item + finalizar/assinar + togglePermissao/toggleAtivo) viram `mutation.mutate(...)` com server fazendo recalculo. EquipeSection bloqueia abertura de modais nos ids virtuais com toast "Membro não editável". **Decisões**: (a) `obra_tarefas.etapaId` é FK opcional (`SET NULL` on delete) — tarefa avulsa fica fora dos grupos por etapa mas continua válida; (b) checklist usa PATCH com `toggleItemId`/`markAllItens` pra evitar round-trip do estado completo no toggle/finalizar; (c) sem mock — flag `NEXT_PUBLIC_ENABLE_EMPREITEIRO_MOCK` segue como gap geral em J11 também. Carries: J11 ganha §13 com follow-ups (notificações de tarefa criada/atribuída, anexos de tarefa, real-time/SSE de checklist toggle pra colaboração). J11 promovida `mock → revisão` no índice.
- 2026-05-25: Task #72 (J06 → pronto) — Medições & Diário de Obra ponta-a-ponta nas 3 personas. **Schema novo** em `shared/db/schema.ts` (4 tabelas + 4 enums + tipos): `obra_etapas` (nome/descricao/ordem/progresso 0-100/status pendente|em_andamento|bloqueado|concluido/responsavel/prazo), `obra_diario` (autor+texto+`fotoFileIds[]`), `obra_ocorrencias` (titulo/descricao/gravidade critico|medio|baixo/status aberta|resolvida/`fotoFileId`/resolvidoPorId+resolvidoEm), `obra_fotos` (fileId+fase antes|durante|agora+tag+enviadaAoContratante). Bootstrap idempotente em `server/bootstrap-medicoes-extras.ts` registrado em `instrumentation.ts`. **Novo kind R2 `obra_foto`** (público, 8MB, JPEG/PNG/WEBP, roles contratante/empreiteiro/superadmin) em `shared/lib/storage/{key-builder.ts,validation.ts}` + commit endpoint + `UploadKind` union em `use-uploads.ts`; ownership anti-tamper na chave `public/obra-fotos/{userId}/<file>`. **Helper** `features/obras/api/access.ts` (`findObraAccess` checa ownership cross-role, `canWriteObraContent` gate role). **8 endpoints novos** (todos requireVerifiedUser + access gate + recordAudit + setNoCacheHeaders): `GET/POST /api/obras/[id]/etapas`, `PATCH/DELETE /[etapaId]` (empreiteiro só `progresso`/`status` — anti-tamper via allowlist; auto-coerência progresso=100→status=concluido), `GET/POST /api/obras/[id]/diario` (autor required, fotoFileIds validados por ownership+kind), `DELETE /[entryId]` (autor ou admin/contratante), `GET/POST /api/obras/[id]/ocorrencias`, `POST /[ocorrenciaId]/resolver` (**UPDATE atômico** `WHERE status='aberta'` → 409 ALREADY_RESOLVED se concorrente), `GET/POST /api/obras/[id]/fotos`, `DELETE /[fotoId]` (autor/contratante/admin + soft-delete `userFiles.deletedAt` + best-effort `deleteObject` no R2). **Hooks** `features/obras/medicoes/hooks/use-obra-j06.ts` (8 queries/mutations TanStack invalidando por `['obras', obraId, kind]`). **4 componentes drop-in** em `features/obras/medicoes/components/` (`EtapasJ06Card`, `DiarioJ06Card`, `OcorrenciasJ06Card`, `FotosJ06Card`) — usam `FileUploader` kind `obra_foto`, confirmam destruções, autor/data em ptBR, gating via props `canWrite` + `canEditScope` (etapas). **Wire-up nas 3 personas**: contratante — `TabEtapas`/`TabTimeline`/`TabOcorrencias`/`TabFotos` reescritos como thin wrappers dos novos cards (canWrite=true; etapas canEditScope=true); admin — 4 novas tabs em `/admin/obras/[id]` (Etapas/Diário/Ocorrências/Fotos read-only, canWrite=false); empreiteiro — novo `<ObraJ06Section>` injetado em `/empreiteiro/minhas-obras/[id]` entre tabs legadas e Financeiro (canWrite=true, canEditScope=false). **Decisões**: (a) bootstrap idempotente sem migração, (b) UPDATE atômico em resolver com 409 vs lock optimista, (c) helper genérico `findObraAccess` resolve admin/contratante/empreiteiro num lugar só, (d) empreiteiro detail page mantém TaskManager/Checklists/Timeline-mock/Cronograma/Documentos/Equipe/Financeiro/Lucro/Saúde em mock — coexistem com BLOCO J06 (carries §13 J06 para tasks futuras: J11 Equipe, J08 Financeiro/Lucro, J09 Documentos, jornada própria de Checklists; AtualizarProgressoModal ainda local — endpoint Task #47 pronto). J06 promovida `revisão → pronto` no índice; §9 marcadas e §13 com 6 bullets datados.
- 2026-05-25: Task #64 (J05 → pronto) — Candidatura & Aceite end-to-end nas 3 personas. **Schema**: `candidaturas` ganhou `observacoes_financeiras`, `motivo_rejeicao`, `mensagem_contratante`, `notificacao_disparada` (bool default false — gancho J13), `cancelada_pelo_empreiteiro` (bool default false), `decidida_em` (timestamp). UNIQUE index `uq_candidaturas_obra_empreiteiro_unique(obra_id, empreiteiro_id)` substitui o check racy do POST — duplicata vira `code=23505` → 409. Bootstrap idempotente em `server/bootstrap-candidaturas.ts` registrado em `instrumentation.ts`. **Endpoints novos**: `POST /api/empreiteiro/candidaturas/[id]/cancelar` (status pendente→rejeitada + flag, reusa enum para evitar migração), `GET /api/contratante/obras/[id]/candidaturas` (JOIN users+empreiteiras, ordenado pendente→aceita→rejeitada + FIFO, anti-enumeração 404), `POST /api/contratante/candidaturas/[id]/aceitar` (transacional com `SELECT … FOR UPDATE` na obra via `tx.execute(sql)`, vincula empreiteira, status→`em_andamento`, rejeita concorrentes pendentes com motivo "Outra proposta foi selecionada", audit `candidaturas.aceitar`), `POST /api/contratante/candidaturas/[id]/rejeitar` (motivo opcional ≤500 chars, ownership transitivo via `clientes.userId`). **POST original refatorado** com anti-tamper (obra publicada + sem empreiteira) e captura 23505. **Frontend contratante**: `CandidaturasCard` reescrito — agora recebe `obraId` + `obraOrcamento`, fetcha via `useCandidaturasObra`, com confirmAprovar/confirmRejeitar dialogs avisando que aprovar é irreversível e rejeita as outras N pendentes; usa `useAceitarCandidatura`/`useRejeitarCandidatura` com invalidação cruzada (`['contratante','candidaturas',obraId]` + detalhe obra). Mapper API→UI gera iniciais/cor (hash determinístico) e parseia `atividades` JSON. Empty state amigável quando sem propostas. **Frontend empreiteiro**: nova página `/empreiteiro/minhas-candidaturas` (`app/empreiteiro/minhas-candidaturas/page.tsx`) com filtros por status, cards com obra+valor+prazo+cidade+motivo rejeição+mensagem aceite, botão "Cancelar" só em pendentes (confirma em dialog) e CTA "Acessar obra" quando aceita. Hook `useMinhasCandidaturas` + `useCancelarCandidatura` em `features/empreiteiro/minhas-candidaturas/`. Item "Minhas Candidaturas" adicionado ao `EMPREITEIRO_NAV_ITEMS`. **GET /api/empreiteiro/candidaturas** agora retorna LEFT JOIN com `obras` (nome/cidade/uf/tipo/visibilidade) — array direto, sem envelope, mantém compat. **Admin**: cobertura via `isAdminLike()` nos 2 endpoints contratante (aceitar/rejeitar) + leitura via /api/admin/obras/[id] (já existia). **Decisões**: (a) reusar enum `rejeitada`+flag em vez de criar `cancelada` (evita migração), (b) usar `SELECT FOR UPDATE` em vez de optimistic lock (mais simples, fila pequena), (c) anexos da candidatura ficam pra J05.B/J13 (form ainda coleta nome/size client-side). **Carries J05**: notificação real ao empreiteiro (flag plantada), anexos da proposta, mock `ENABLE_MOCK` em `aplicar/page.tsx` (decisão prévia do usuário). J05 promovida `parcial → pronto` no índice.
- 2026-05-25: Task #62 (J04 → pronto) — `NovaObraCard` ganhou badge "📎 N anexo(s)" (só quando >0), chips de modalidade (`Administração` / `Empreitada global` / `Empreitada por etapa`) e materiais (`Materiais: Contratante/Empreiteiro/Misto`), e rótulo "Orçamento" virou "Orçamento estimado". Backend: `GET /api/obras` adiciona `anexosCount` via subquery escalar correlacionada (`SELECT COUNT(*)::int FROM obra_anexos JOIN user_files ... WHERE obra_id = obras.id AND user_files.deleted_at IS NULL`) — usa índice `obra_anexos(obra_id)`, payload leve, sem alterar paginação ou envelope `{ rows, total, ... }`. Adapter `dbToNovaObra` mapeia `modalidade` e `anexosCount` (default 0); type `NovaObra` ganha os campos opcionais. Item "estado bloqueado por `perfilCompleto`" da §9 J04 marcado retroativamente — já vivia desde a #43 via `usePerfilStatus` + `BlockedBanner`. Flag `NEXT_PUBLIC_ENABLE_EMPREITEIRO_MOCK` segue como gap aberto (compartilhada com dashboard/minhas-obras). J04 promovida `revisão → pronto` no índice.
- 2026-05-25: Task #53 (J08) — job idempotente `markOverduePagamentos()` em `features/financeiro/mark-overdue-job.ts` faz `UPDATE financeiro SET status='atrasado' WHERE status='pendente' AND data_vencimento < CURRENT_DATE` e grava audit log `financeiro.mark-overdue` (ip="cron") com a lista de IDs atualizados. CLI `scripts/mark-overdue-pagamentos.ts` (`npx tsx scripts/mark-overdue-pagamentos.ts`) pensada pra Replit Scheduled Deployment diário. Também executa em `instrumentation.ts` no boot do server, então o status fica consistente cross-persona mesmo sem o cron configurado — o GET do empreiteiro (`app/api/empreiteiro/pagamentos`) agora reflete `atrasado` direto do campo bruto. Derivação read-time mantida em `app/api/contratante/pagamentos` como safety net.
- 2026-05-25: Task #45 (J04) — `/empreiteiro/novas-obras` migrou de paginação shadcn para infinite scroll. Novo hook `useNovasObrasInfinite` (TanStack `useInfiniteQuery`) em `features/empreiteiro/novas-obras/hooks/use-novas-obras.ts` com `getNextPageParam` consumindo `{ page, totalPages }`. Página usa `IntersectionObserver` (rootMargin 300px) + sentinel + spinner inline; fallback "Carregar mais" se o observer não disparar. Filtros server-side resetam a lista automaticamente pelo `queryKey` (params no key). `useNovasObras` (one-shot) preservada para outros callsites.
- 2026-05-25: Task #43 (J04.C + carry J03). **Empreiteiro**: `/empreiteiro/novas-obras` reescrita com filtros server-side (cidade/tipo/materiaisPor/orcamento via query params) + paginação shadcn; filtros status/complexidade/busca seguem client-side sobre a página atual. Favoritos migrados pra `obras_salvas`: `features/empreiteiro/obras-salvas/{api,hooks}` com `useObrasSalvas`, `useObrasSalvasIds`, `useToggleObraSalva` (optimistic + rollback). `NovaObraCard` ganhou bookmark inline. `/empreiteiro/obras-salvas` consome a API real com empty state + busca local. Store Zustand `obras-salvas-store.ts` **deletado**; detalhe empreiteiro migrado via shim `toggleSave/isSaved`. **Contratante**: `ObraVisibilidadeActions` novo no detalhe (`app/contratante/minhas-obras/[id]/page.tsx`) com botões Publicar/Pausar/Republicar/Arquivar (PATCH `/api/obras/[id]` body `{visibilidade}`) + uploader de anexos (`FileUploader` kind `obra_anexo` → POST `/api/obras/[id]/anexos`) + listagem com download (signed URL) e remoção. **Admin**: `/admin/obras/[id]` migrada pra API real — novo `features/admin/obras/api/admin-obra-detalhe-service.ts` (adapter `AdminObraApiResponse → AdminObraDetalhe`, mapeando audit logs em `historico`) + `useAdminObraDetalhe`. Badge de visibilidade adicionada ao hero. Mock `features/admin/obras/mocks/index.ts` deletado. J03 promovida pra `pronto`, J04 pra `revisão` no índice; §9 marcadas e §13 com bullets datados em ambas as jornadas.
- 2026-05-25: Task #42 (J04.B) — backend marketplace paginado + obras-salvas. `GET /api/obras` agora devolve envelope `{ rows, total, page, pageSize, totalPages }` (clamp pageSize 1..100, default 20). Filtros novos: `cidade` ILIKE `%x%` (case-insensitive), `materiaisPor`, `tipo`. Empreiteiro: predicate `NOT EXISTS (candidatura sua)` usa `idx_candidaturas_obra_empreiteiro` (anti-self-apply). 3 endpoints novos: `GET/POST /api/empreiteiro/obras-salvas` e `DELETE /api/empreiteiro/obras-salvas/[obraId]` — POST `ON CONFLICT DO NOTHING` (idempotente), DELETE silencioso 200 (anti-oracle), favoritar `rascunho` ⇒ 404 (anti-enum), 403 para contratante/admin. Rate-limit 30/user + 60/ip por min em writes. Audit `obras-salvas.add|remove`. 6 callsites consumidores atualizados para `data?.rows` com `pageSize: 100` (mantém paginação client-side atual). Hook órfão `features/obras/hooks/use-obras.ts` + `features/obras/types/index.ts` deletados. Smoke `scripts/smoke-task42-marketplace.ts` cobre os 12 critérios da §10 J04 (12/12 ✓). J04§9 checklist marcado, §13 com 3 bullets datados (envelope renomeado, busca textual server-side adiada, paginação server-side em J04.C).
- 2026-05-25: Task #41 (J04.A) — fundação do Marketplace & Descoberta. Tabela `obras_salvas` (id, userId FK CASCADE, obraId FK CASCADE, createdAt, UNIQUE(userId,obraId)) + `insertObraSalvaSchema` em `shared/db/schema.ts`. Bootstrap idempotente `server/bootstrap-marketplace.ts` registrado em `instrumentation.ts` (mesmo pattern de #32): cria tabela + índices `uq_obras_salvas_user_obra`, `idx_obras_salvas_user_id`, e ainda `idx_candidaturas_obra_empreiteiro` + `idx_candidaturas_status` (estes últimos destravam a query anti-self-apply de J04 e o ranking de J05 — auditoria de #41 confirmou que `candidaturas` antes só tinha PK). Doc J04 reescrita ponta-a-ponta (12 critérios, cross-refs, riscos atualizados). Bullets cross-jornada em J02§13 (gap especialidades/zonaAtuacao) e J05§13 (índices herdados). Convenção `visibilidade='publicada'` vs `status` execução mantida. Endpoints e UI ficam nas tasks-filhas #42 (J04.B) e #43 (J04.C).
- 2026-05-25: Task #32 (J03.A) — schema de Obras estendido (13 colunas novas em `obras` + 4 enums + tabela `obra_anexos` + 4 índices). Bootstrap idempotente em `server/bootstrap-obras.ts` registrado em `instrumentation.ts` (mesmo pattern de `bootstrap-storage`). Backfill: linhas pré-existentes com `empreiteira_id IS NOT NULL` OU `status<>'planejamento'` viraram `visibilidade='publicada'`; resto ficou `rascunho`. `insertObraSchemaStrict` em `features/obras/schemas/index.ts` com `superRefine` condicional: rascunho exige só nome+endereco; publicada exige tipo/descricao(≥20)/cep/cidade/uf/modalidade/materiaisPor. Sem mudanças em UI/endpoints (vão pra #33). J02 promovida pra `revisão`. Docs J03/J04/J13/02/README atualizados. Convenção `status` (execução) vs `visibilidade` (marketplace) — dimensões ortogonais, ambos têm `pausada` intencionalmente.
- 2026-05-25: Task #31 (J02) — superadmin liberado nos 5 endpoints administrativos que faziam gate `role !== "admin"` literal (perfil/admin, /admin/configuracoes, /admin/integracoes/api-key, /admin/clientes/[id]/aprovacao, /admin/empreiteiras/[id]/aprovacao). Helper `isAdminLike()` adicionado em `features/auth/api/auth-utils.ts`. Aba **Perfil** do `/admin/configuracoes` deixa de ficar em Skeleton infinito para superadmins. Convenção "Convenções de planejamento" + cabeçalho `> Jornada: JXX` nos plan files. Jornada 02 atualizada (checklist + gaps).
- 2026-05-13: Task #26 (rev2) — schema completo: `users.avatar_file_id`, `empreiteiro_portfolio` (titulo/ordem) e `empreiteiro_documentos.status`. Commit grava `avatarFileId`, insere portfolio rows, e faz **replace-by-tipo** nos documentos (soft-delete da versão anterior). UI empreiteiro refeita: portfólio em grade de cards (título editável + reorder ↑/↓ + remover), documentos agrupados por **tipo obrigatório** (CNPJ, Alvará, Certidão Negativa, Outros) com badge `enviado`/`pendente` e botão Substituir/Enviar/Baixar/Remover. `/api/uploads/sign` aceita `?key=` (além de `?id=`); `/api/uploads/presign` aceita `filename` (compat com `originalName`). Avatar trocado refresca o store de auth para refletir no topbar/sidebar.
- 2026-05-13: Task #26 — integração Cloudflare R2 + uploads (avatar/portfolio público, documentos privados via URL assinada). Aba Documentos no empreiteiro, uploaders de avatar nas três visões.
- 2026-05-12: Task #20 — super admin auto-promovido, aba Usuários CRUD, modo Ver como (read-only), troca obrigatória de senha 1º login, fix do cadastro silencioso (toast + erros inline), bloqueio de login para conta inativa.
- 2026-05-10: Transactional email migrated to Brevo (HTTP API, secret `BREVO_API_KEY`). Same function signatures in `shared/lib/email.ts`, same templates, same `EMAIL_TEST_MODE` capture path. Legacy `lib/email.ts` and previous provider package/secret removed.
- 2026-02-21: Built complete admin internal views (Clientes, Empreiteiras, Caixa, Entradas, Saídas, Anúncios, FAQ with full CRUD forms)
- 2026-02-20: Built complete contratante internal views (Minhas Obras, Detalhes da Obra, Nova Obra, Pagamentos, xchat, FAQ)
- 2026-02-20: Built complete empreiteiro internal views (Minhas Obras, Novas Obras, Detalhes da Obra, xchat, FAQ)
- 2026-02-13: Migrated from Vite+Express to Next.js 16 App Router
- All API routes migrated to Next.js Route Handlers
- All pages migrated to App Router with proper client/server component separation
- Tailwind CSS v4 with PostCSS plugin configured
- Auth flow: login returns { user } wrapper, /me returns user directly
- E2E tests passed: login, dashboard navigation, CRUD pages, empreiteiro features
- Old Vite/Express files cleaned up

## Replit Auth Notes
- Keep a stable `SESSION_SECRET` in Replit Secrets (required for JWT validation consistency across restarts)
- Access the app through a single canonical public URL during auth tests (avoid switching between preview/public domains)
- Login, refresh and protected APIs rely on `access_token` and `refresh_token` httpOnly cookies
- If `POST /api/auth/refresh` returns 401, validate if `refresh_token` is present in request cookies first
